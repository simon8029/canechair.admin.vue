import axios from 'axios';

const state = {
    name: "Simon X.L.",
    picture: require("@assets/img/authors/prf4.jpg"),
    job: "Full Stack JS"
}
const mutations = {
}

const actions = {
}

const getters = {
}

const userModule = {
    state,
    mutations,
    actions,
    getters
}

export default userModule;